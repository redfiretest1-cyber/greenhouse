import React from "react";
import ReactDOM from "react-dom/client";
import GreenhouseDashboard from "./GreenhouseDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GreenhouseDashboard />
  </React.StrictMode>
);
